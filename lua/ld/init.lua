return {
  filter = require('ld.filter'),
  forEach = require('ld.forEach'),
  map = require('ld.map'),
  some = require('ld.some'),
  reduce = require('ld.reduce'),
  every = require('ld.every'),
  concat = require('ld.concat'),
  debounce = require('ld.debounce'),
}
